# 공백 처리 개선 사항

## 문제점
기존 공백(빈칸) 탐지 로직에서 다음과 같은 문제가 발생했습니다:

1. **빈 답지의 밀도가 너무 높게 계산됨**: 실제로 빈칸인 답지들의 잉크 밀도가 0.024~0.026으로 측정되어, 기존 임계값 0.01을 초과
2. **빈 답지가 그룹화됨**: 밀도가 임계값을 초과한 빈 답지들이 빈칸으로 분류되지 않고, 대신 서로 유사하다고 판단되어 그룹화됨
3. **뒷장(파일명 *2) 빈 답지 탐지 실패**: 특히 파일명이 2로 끝나는 빈 답지들이 제대로 탐지되지 않음

## 원인 분석
`ink_density()` 함수가 다음과 같은 이유로 과도하게 민감했습니다:

1. **노이즈 감지**: 종이의 질감, 그림자, 스캔 노이즈 등을 잉크로 잘못 인식
2. **Sauvola 임계값 방법의 과민성**: 로컬 임계값 방법이 작은 변화에도 민감하게 반응
3. **불충분한 필터링**: 작은 노이즈 픽셀들이 제거되지 않고 밀도 계산에 포함됨

## 해결 방법

### 1. 전처리 개선
```python
# 더 강한 가우시안 블러 적용 (3x3 → 5x5)
roi = cv2.GaussianBlur(roi, (5, 5), 0)
```
- 노이즈와 종이 질감을 부드럽게 만들어 제거
- 실제 잉크 영역은 유지하면서 미세한 변화는 제거

### 2. Sauvola 파라미터 조정
```python
# k: 0.2 → 0.35 (덜 민감하게)
# window_size: 25 → 51 (로컬 변화에 덜 민감하게)
th = threshold_sauvola(roi, window_size=51, k=0.35)
```
- `k` 값 증가: 임계값이 배경과 전경을 더 명확하게 구분
- `window_size` 증가: 더 넓은 영역을 고려하여 로컬 노이즈에 덜 민감

### 3. 모폴로지 연산 강화
```python
# 커널 크기 증가 (2x2 → 3x3)
kernel = cv2.getStructuringElement(cv2.MORPH_ELLIPSE, (3, 3))
binary = cv2.morphologyEx(binary, cv2.MORPH_OPEN, kernel)
# 추가 closing 연산
binary = cv2.morphologyEx(binary, cv2.MORPH_CLOSE, kernel)
```
- **Opening**: 작은 노이즈 점들 제거
- **Closing**: 실제 잉크 영역 내의 작은 구멍 채우기

### 4. 임계값 상향 조정
```python
blank_density_thresh: float = 0.02
```
- 개선된 노이즈 필터링으로 실제 빈칸의 밀도가 낮아질 것으로 예상
- 0.01 → 0.02로 증가하여 약간의 노이즈가 있는 빈칸도 탐지

## 예상 효과

### Before (기존)
```
1000292.JPG, 0.0063, True   ✓ (정확)
1000202.JPG, 0.0244, False  ✗ (빈칸인데 False)
1000282.JPG, 0.0251, False  ✗ (빈칸인데 False)
1000322.JPG, 0.0238, False  ✗ (빈칸인데 False)
```

### After (개선 후 예상)
```
1000292.JPG, ~0.004, True   ✓ (노이즈 감소로 더 낮은 밀도)
1000202.JPG, ~0.012, True   ✓ (빈칸으로 정확히 탐지)
1000282.JPG, ~0.013, True   ✓ (빈칸으로 정확히 탐지)
1000322.JPG, ~0.011, True   ✓ (빈칸으로 정확히 탐지)
```

## 테스트 방법
파이프라인을 다시 실행하여 다음을 확인:
1. `images_summary.csv`에서 빈칸 답지들의 밀도 값 확인
2. `blank_answers` 폴더에 이전에 그룹화되었던 빈칸들이 제대로 분류되었는지 확인
3. `grouped` 폴더에 빈칸들이 더 이상 그룹화되지 않는지 확인

## 주의사항
- 이 변경사항은 기존에 올바르게 탐지되던 빈칸에는 영향을 주지 않습니다
- 실제 필기가 있는 답지의 밀도는 여전히 0.02보다 훨씬 높아야 합니다
- 만약 일부 연하게 쓴 답지가 빈칸으로 잘못 분류된다면, `blank_density_thresh`를 0.015 정도로 낮출 수 있습니다

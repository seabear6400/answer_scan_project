# 핵심 라이브러리
numpy
opencv-python
pillow
torch
torchvision
scikit-learn
pandas
polars

# GUI/대시보드
streamlit
tkinter  # 주의: 대부분 Python 표준 라이브러리이지만 일부 Linux 배포판에서는 별도 설치 필요

# 이미지 해시/유사도
imagehash

# 그래프/네트워크 알고리즘 (매칭)
networkx>=3.2

# 진행률 표시
tqdm

# 시스템 정보/성능 모니터링
psutil
platform  # 표준 라이브러리

# 고급 딥러닝 모델 (옵션)
timm  # DINOv2 등을 위한 Vision Transformer 모델

# 고급 기능 (옵션이지만 권장)
lpips  # 이미지 유사도 측정
scikit-image  # Sauvola threshold 등 고급 이미지 처리
paddleocr  # OCR 텍스트 인식
rapidfuzz  # 빠른 문자열 유사도

# 해시 (옵션)
pdqhash  # PDQ hash for image deduplication

# ANN 검색 백엔드 (옵션 - 성능 향상)
faiss-cpu  # CPU 버전 (GPU 사용시 faiss-gpu로 변경)
hnswlib  # 메모리 효율적인 ANN

# 대시보드 확장 (옵션 - UI 개선)
streamlit-image-comparison

# 파일 처리/병렬화
joblib  # 병렬 처리
concurrent.futures  # 표준 라이브러리
threading  # 표준 라이브러리
pathlib  # 표준 라이브러리
hashlib  # 표준 라이브러리

# 기타 유틸리티
argparse  # 표준 라이브러리
logging  # 표준 라이브러리
os  # 표준 라이브러리
sys  # 표준 라이브러리
shutil  # 표준 라이브러리
stat  # 표준 라이브러리
time  # 표준 라이브러리
re  # 표준 라이브러리
glob  # 표준 라이브러리
csv  # 표준 라이브러리
subprocess  # 표준 라이브러리
itertools  # 표준 라이브러리
warnings  # 표준 라이브러리
io  # 표준 라이브러리
dataclasses  # 표준 라이브러리 (Python 3.7+)
typing  # 표준 라이브러리
collections  # 표준 라이브러리

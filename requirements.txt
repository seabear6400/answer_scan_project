# 핵심/권장 의존성
# (표준 라이브러리는 requirements에 포함할 필요가 없습니다.)

# 핵심 라이브러리
numpy
opencv-python
Pillow
torch
torchvision
scikit-learn
pandas
polars

# 이미지 처리 / 유틸
imagehash
tqdm
psutil
scikit-image  # Sauvola 등 고급 이미지 처리 (권장)

# 대시보드 / GUI
streamlit
# tkinter은 표준 라이브러리로 보통 pip 설치 불필요 (OS에 따라 별도 패키지 필요할 수 있음)

# 그래프/매칭
networkx

# 병렬 처리/유틸(선택)
joblib

# 선택적/옵션 패키지 (비교적 무겁거나 환경 의존적이므로 주석 처리)
# timm           # DINOv2 등 고급 ViT 모델이 필요하면 활성화
# lpips          # 이미지 유사도 측정 (옵션)
# paddleocr      # OCR (옵션, 설치시 추가 시스템 의존성 있음)
# rapidfuzz      # 빠른 문자열 유사도
# pdqhash        # PDQ 해시 (옵션)
# faiss-cpu      # ANN (CPU) — 성능 향상용 (옵션)
# hnswlib        # 메모리 효율적인 ANN (옵션)
# streamlit-image-comparison  # 대시보드 확장 (옵션)

# 노트:
# - 표준 라이브러리(예: pathlib, threading, argparse 등)는 requirements에 포함할 필요가 없습니다.
# - GPU용 PyTorch는 플랫폼(윈도우/리눅스)과 CUDA 버전에 따라 설치 명령이 다릅니다.
#   공식 안내를 참고하여 `torch`/`torchvision` 설치 명령을 조정하세요.
